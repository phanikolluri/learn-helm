---
apiVersion: v1
kind: ConfigMap
metadata:
  name: helm-01
  {{- template "mychart.labels" }}
data:
  {{- with .Values.favorite }}
  drink: {{ .drink | default "tea" | quote }}
  food: {{ .food | upper | quote }}
    {{- end }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: helm-02
  {{- template "mychart.labels" }}
data:
  {{- with .Values.favorite }}
  drink: {{ .drink | default "tea" | quote }}
  food: {{ .food | upper | quote }}
    {{- end }}






















                      







